# 队员 A 开发记录：韦罗权

## v01

提交主题：主线程框架：补充 main 入口和模块初始化占位

本次修改建立系统主入口框架，在 `main.c` 中加入 RT-Thread 项目启动入口，并预留硬件层和线程层初始化调用位置。

## v02

提交主题：GPIO配置：新增 LED/按键/蜂鸣器引脚宏

本次修改在驱动层头文件中补充 LED、按键和蜂鸣器相关引脚宏，方便后续统一管理硬件资源。

## v03

提交主题：LED驱动：完成LED初始化与亮灭控制接口

本次修改完善 LED 控制接口，实现 LED 初始化、亮灯、灭灯和状态切换等基础驱动功能。

## v04

提交主题：呼吸灯驱动：加入软件PWM占空比输出函数

本次修改加入软件 PWM 输出逻辑，通过占空比控制 LED 亮灭比例，为呼吸灯效果提供底层支持。

## v05

提交主题：串口调试：加入硬件层版本号和初始化日志

本次修改加入驱动层版本号和初始化日志输出，方便调试时确认硬件驱动模块是否正常启动。

## v06

提交主题：按键驱动：加入按键读取接口和消抖说明

本次修改加入按键读取接口，并说明按键消抖处理思路，为后续按键扫描线程提供输入来源。

## v07

提交主题：蜂鸣器驱动：加入报警输出控制接口

本次修改加入蜂鸣器控制接口，实现报警输出的开启和关闭，为报警线程提供硬件控制函数。

## v08

提交主题：异常保护：加入占空比边界检查和参数保护

本次修改对呼吸灯占空比参数加入边界检查，避免异常参数导致 LED 输出逻辑错误。

## v09

提交主题：代码整理：优化驱动层注释和宏定义命名

本次修改整理驱动层注释和宏定义命名，使代码结构更清晰，便于队友调用和报告说明。

## v10

提交主题：驱动定版：整理硬件驱动接口供功能线程调用

本次修改集中在系统主线程与硬件驱动层，主要用于支撑后续 LED 呼吸灯线程、按键扫描线程和报警线程调用。
