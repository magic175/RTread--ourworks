# 队员 B 开发记录：巩长杞

## v01

提交主题：线程框架：新增功能线程头文件和事件枚举

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v02

提交主题：按键线程：创建按键扫描线程和消抖流程

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v03

提交主题：呼吸灯线程：创建LED亮度周期变化逻辑

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v04

提交主题：报警线程：创建蜂鸣器报警处理线程

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v05

提交主题：消息队列：实现按键事件在线程间传递

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v06

提交主题：信号量同步：加入按键触发唤醒机制

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v07

提交主题：互斥量保护：保护报警状态共享变量

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v08

提交主题：长按逻辑：补充按键长按与短按区分

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v09

提交主题：调试输出：补充线程运行日志和状态说明

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。

## v10

提交主题：功能定版：整理线程模块接口并准备合并

本次修改集中在功能线程、线程间消息传递和报警逻辑，主要用于实现按键事件驱动的报警处理。
